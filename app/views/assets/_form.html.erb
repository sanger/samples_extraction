<%= bootstrap_form_for(@asset) do |f| %>
  <% if @asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset.errors.count, "error") %> prohibited this asset from being saved:</h2>

      <ul>
      <% @asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div data-psd-component-class="ConditionGroup">
  <div class="facts">
  </div>
  <div data-psd-component-class="FactReader" data-psd-component-parameters="<%= {'template' => 'templates/fact_reader', 'actionType' => 'addFacts'}.to_json %>">
  </div>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
