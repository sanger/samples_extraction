<p id="notice"><%= notice %></p>

<p>
  <strong>Instrument Barcode:</strong>
  <%= @instrument.barcode %>
</p>

<%= form_tag(activities_path, action: 'create') do %>
  <input type="hidden" name="instrument_barcode" value="<%= @instrument.barcode %>" />
  <%= render :partial => 'kit_barcode_reader' %>
  <%= submit_tag("Create activity") %>
<% end %>



<%= link_to 'Edit', edit_instrument_path(@instrument) %> |
<%= link_to 'Back', instruments_path %>
